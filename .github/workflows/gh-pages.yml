name: GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: "pnpm"
          # 直接指向根目錄的鎖定檔，解決 "unable to cache" 報錯
          cache-dependency-path: "pnpm-lock.yaml"

      - name: Install dependencies
        # 確保使用 frozen-lockfile 以保證環境一致性
        run: pnpm install --frozen-lockfile

      - name: Run WebP Conversion Script
        run: node .github/scripts/convert_webp.js

      - name: Build
        # Docusaurus 預設產出路徑為 ./build
        run: pnpm build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"
          commit_message: "docs: deploy to gh-pages"

      - name: Sync optimized images back to main
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add finance/ blog/ docs/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: optimize images to webp [skip ci]"
            git push origin main
          fi
